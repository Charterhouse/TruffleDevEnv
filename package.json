{
  "name": "truffle-devenv",
  "version": "0.1.0",
  "description": "A complete development environment for developing Ethereum Dapps, including client-side and server-side javascript.",
  "main": "index.js",
  "repository": "https://github.com/Charterhouse/TruffleDevEnv.git",
  "contributors": [
    "Stefan van den Oord",
    "Mark Spanbroek"
  ],
  "license": "MIT",
  "devDependencies": {
    "standard": "^8.6.0"
  },
  "scripts": {
    "postinstall": "local_modules install -f",
    "devsetup": "yarn install && local_modules link -f && cd local_modules/contracts && yarn install",
    "test": "standard && ( cd local_modules/contracts && yarn test ) && ( cd local_modules/webapp && yarn test )"
  },
  "standard": {
    "globals": [
      "artifacts",
      "contract"
    ]
  },
  "dependencies": {
    "local_modules": "^1.0.8"
  }
}
